# build output
dist/
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables (NUNCA commitear — Replit Secrets en producción)
.env
.env.local
.env.development
.env.production

# Database (SQLite) — vive en .data/, persiste local + Replit, no se sube a git
.data/
*.db
*.db-journal
*.db-wal
*.db-shm

# Media uploads via CMS (los WebPs del brand sí se commitean, pero los uploads runtime no)
public/images/cms/

# editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/
